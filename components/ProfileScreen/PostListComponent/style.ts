import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    postContainer: {
        flex: 1,
        margin: 3,
    },
    postImage: {
        height: 180,
        resizeMode: 'cover',
        borderRadius: 10
    }
})

export default styles
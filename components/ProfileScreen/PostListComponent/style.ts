import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    postContainer: {
        height: 200,
        flex: 1,
    },
    postImage: {
        height: 200,
    }
})

export default styles